import React from 'react';
import "./animate.css";

const Loading = () => (
  <div className="Box fixed pin-t pin-r m-6 bg-yellow-lightest px-4 py-3 z-50 text-center text-lg font-bold">
    Loading…
  </div>
);

export default Loading;
